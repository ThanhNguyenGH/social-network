
<div class="container mx-auto p-4">
  <h1 class="text-2xl font-bold text-red-600">Error</h1>
  <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mt-4">
    <p class="font-bold">Something went wrong!</p>
    <p><%= message || 'An unexpected error occurred.' %></p>
    <div class="mt-4">
      <a href="/" class="inline-block bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Back to Home</a>
      <% if (user && user._id) { %>
        <a href="/users/profile/<%= user._id %>" class="inline-block bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 ml-2">Back to Profile</a>
      <% } %>
    </div>
  </div>
</div>